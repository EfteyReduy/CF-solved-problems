#include<bits/stdc++.h>
using namespace std;

long long f(string x)
{
    int ind=x.length();
    long long total=0;




    return total;
}

int main()
{
    long long l, r;
    string a, b;
    cin>>a>>b;
    l=f(a);
    r=f(b);

    if(a[0]==a[a.length()-1])
    {
        l--;
    }
    cout<<r-l<<endl;
}
