#include<bits/stdc++.h>
#define F first
#define S second

using namespace std;


main()
{
    int n;
    cin>>n;
    vector<pair<int, pair<int, int> > >p;

    for(int i=0; i<n; i++)
    {
        int a, b;
        cin>>a>>b;

        p.push_back({a, {i, 1});
        p.push_back({b, {i, 2})
    }
    int ans1[n], ans[n];

    sort(p.begin(), p.end());



}
